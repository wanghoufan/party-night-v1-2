export type {
  ActiveRound,
  BoundaryProfile,
  GameCard,
  GamePackDefinition,
  GameSession,
  Intensity,
  Player,
  RoundHistory,
  SessionConfig,
} from "@/lib/domain/schemas";

export type RandomSource = () => number;
