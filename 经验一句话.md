V1.5（转瓶子链回跳+切包1/40重计+顶栏暂停结束+题库导出180卡7文件）教训：E2E旧轮次断言须随产品语义走，行为变更先改测试口径。
V1.6（终稿350入库+指数陡坡+L1L2不断游+旧题面25补遗）经验：终稿先定配额再写题面，旧题面补遗只改卡面不碰id/强度/说明/boundary。
终稿包与软件同源（350/3-5-7-14-21/35互动15了解0看戏），审计表与终稿表分家（把关存旧180、终稿只写350）。
V2 AI直连分块（4x10/单批45s）举证：max_tokens 按 10卡x320 估会在强度5截断成 Unterminated string 致整批失败，须与服务端同口径 max(8192,卡数x640) 起步并加批重试；矩阵 harness 的 URL 断言必须兼容 trailingSlash，非法人格(players<minPlayers)要在生成层 SKIP 不进分母。
Change B 负向边界探测（3.3 pointing-game@2 与 most-likely@2）实战教训：把非法组合单列成 NEGATIVE_BOUNDARY_PROBE、不进通过率分母，才能真抓到漏洞——本轮就抓出「AI 卡自报 minPlayers 绕过 pack 契约」导致 2 人局拿到 3 人玩法卡；人数下限唯一真源必须是 pack 契约 max(卡自报, pack.minPlayers)，并在服务端与 App 共用同一漏斗。
